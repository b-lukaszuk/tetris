import { HighScoresSortPipe } from './high-scores-sort.pipe';

describe('HighScoresSortPipe', () => {
  it('create an instance', () => {
    const pipe = new HighScoresSortPipe();
    expect(pipe).toBeTruthy();
  });
});
