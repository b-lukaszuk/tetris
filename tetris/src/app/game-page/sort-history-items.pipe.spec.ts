import { SortHistoryItemsPipe } from './sort-history-items.pipe';

describe('SortHistoryItemsPipe', () => {
    it('create an instance', () => {
        const pipe = new SortHistoryItemsPipe();
        expect(pipe).toBeTruthy();
    });
});
